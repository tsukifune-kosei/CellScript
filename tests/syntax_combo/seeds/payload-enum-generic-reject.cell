// audit: phase=reject_compile
// audit: contains=cannot be hidden in ordinary generic layout
module cellscript::audit::seed_payload_enum_generic_reject

resource Token has consume {
    amount: u64,
}

private enum Boxed<T> {
    Empty,
    Some(T),
}

fn hide(input: Token) -> Boxed<Token> {
    Boxed::Some<Token>(input)
}
